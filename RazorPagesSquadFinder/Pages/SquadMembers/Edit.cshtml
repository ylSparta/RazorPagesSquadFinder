@page
@model RazorPagesSquadFinder.Pages.SquadMembers.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>SquadMember</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SquadMember.SquadMemberId" />
            <div class="form-group">
                <label asp-for="SquadMember.SquadId" class="control-label"></label>
                <select asp-for="SquadMember.SquadId" class="form-control" asp-items="ViewBag.SquadId"></select>
                <span asp-validation-for="SquadMember.SquadId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SquadMember.MemberId" class="control-label"></label>
                <select asp-for="SquadMember.MemberId" class="form-control" asp-items="ViewBag.MemberId"></select>
                <span asp-validation-for="SquadMember.MemberId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
